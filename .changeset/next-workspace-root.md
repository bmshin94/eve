---
"eve": patch
---

Install Web Chat as a peer application under `apps/web` for standalone agents and agent workspaces, with guided Next.js or Vercel service hosting. `withEve()` can discover either project shape from an explicit `eveRoot`.
