---
"eve": patch
---

Keep Web Chat attached for background task results, including results that arrive during another send, without rendering runtime-authored task input as a user message. Optimistic messages now reconcile with their server delivery identity instead of stream order.
